server:
  port: 9001

spring:
  application:
    name: cloud-user-service
  datasource:
    url: jdbc:mysql:///jmc
    username: root
    password: root

management:
  endpoints:
    web:
      exposure:
        include: '*'
